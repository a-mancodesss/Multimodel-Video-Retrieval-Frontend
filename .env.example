# Optional: full API origin (omit to use Vite dev proxy at /api → http://127.0.0.1:5050)
# VITE_API_BASE_URL=http://127.0.0.1:5050

# When set, users must enter this password before using the app (stored session in localStorage)
# VITE_AUTH_PASSWORD=
